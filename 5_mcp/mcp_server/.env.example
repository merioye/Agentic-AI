# Only required if you want to run the server over streamable HTTP with
# auth enabled. Leave unset for local stdio dev — stdio connections are
# implicitly trusted since the client spawned the process itself.
DEVOPS_MCP_TOKEN=
MCP_ISSUER_URL=https://internal.local
MCP_TRANSPORT=stdio
